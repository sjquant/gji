# gji Development Guidelines

## Active Technologies

- TypeScript on Node.js 20 LTS + Local Git CLI availability, terminal interactive prompt support, shell environment with `zsh` for post-setup execution

## Project Structure

```text
src/
tests/
```

## Commands

pnpm test && pnpm lint
